export interface ICategory {
  id: string;
  category: string;
}
export interface IFAQ {
  id: string;
  category: string;
  title: string;
  content: string;
}
export interface IChangelog {
  id: string;
  appVersion: string;
  releaseDate: string;
  changes: string;
}
